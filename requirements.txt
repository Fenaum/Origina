
# Web framework: this module is for building the FastAPI application
fastapi

# ASGI server: this module is required to run the FastAPI application
uvicorn[standard]

# ORM / DB: these modules are for interacting with the database and managing database connections. SQLAlchemy is the ORM (Object-Relational Mapping) library, and psycopg2-binary is the PostgreSQL adapter for Python.
sqlalchemy>=2.0
psycopg2-binary

# Migrations
alembic

# Validation / settings
pydantic[email]
pydantic-settings
python-dotenv

# Auth
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
python-multipart

# Optional but recommended (logging & typing helpers)
typing-extensions

# ─────────────────────────────────────────────────────────────────────────────
# Testing (ROADMAP.md "Testing Checkpoints" §A.1)
# ─────────────────────────────────────────────────────────────────────────────
# `httpx` is required by both the production app (for outbound calls) and
# by the test client (ASGITransport). Pin >= 0.27 because ASGITransport
# moved into the main package in 0.27.
httpx>=0.27
# `pytest` + `pytest-asyncio` power the backend test suite. We pin
# `asyncio_mode=auto` in pytest.ini so tests don't need @pytest.mark.asyncio
# everywhere.
pytest>=8.0
pytest-asyncio>=0.23
# Coverage is optional in dev but the CI gate uses it (see ROADMAP §D).
pytest-cov>=5.0
